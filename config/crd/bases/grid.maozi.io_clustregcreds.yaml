---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clustregcreds.grid.maozi.io
spec:
  group: grid.maozi.io
  names:
    kind: ClustRegCred
    listKind: ClustRegCredList
    plural: clustregcreds
    shortNames:
      - crc
    singular: clustregcred
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.registry
          name: Registry
          type: string
        - jsonPath: .spec.secretName
          name: SecretName
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ClustRegCred is the Schema for the clustregcreds API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
              type: string
            metadata:
              type: object
            spec:
              description: ClustRegCredSpec defines the desired state of ClustRegCred
              properties:
                email:
                  default: ""
                  description: Email associated with the container registry account
                  type: string
                password:
                  description: Password for authenticating to the container registry
                  minLength: 1
                  type: string
                registry:
                  description: Registry is the container registry URL (e.g., docker.io, gcr.io)
                  minLength: 1
                  type: string
                secretName:
                  description: SecretName is the name to be used for the image pull secret in each namespace
                  minLength: 1
                  type: string
                username:
                  description: Username for authenticating to the container registry
                  minLength: 1
                  type: string
              required:
                - password
                - registry
                - secretName
                - username
              type: object
            status:
              description: ClustRegCredStatus defines the observed state of ClustRegCred
              properties:
                conditions:
                  description: Conditions represent the latest available observations of ClustRegCred's state
                  items:
                    description: "Condition contains details for one aspect of the current state of this API Resource."
                    properties:
                      lastTransitionTime:
                        description: lastTransitionTime is the last time the condition transitioned from one status to another.
                        format: date-time
                        type: string
                      message:
                        description: message is a human readable message indicating details about the transition.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: observedGeneration represents the .metadata.generation that the condition was set based upon.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                lastSyncTime:
                  description: LastSyncTime is the timestamp of the last successful sync
                  format: date-time
                  type: string
                syncedNamespaces:
                  description: SyncedNamespaces is the list of namespaces where the secret has been synced
                  items:
                    type: string
                  type: array
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}